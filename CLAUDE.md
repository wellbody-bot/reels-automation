# CLAUDE.md — 릴스 자동화 프로젝트 요약

이 파일은 Claude Code가 세션 시작 시 자동으로 읽습니다. 전체 계획은
`reels_automation_project_plan.md`를 참조하세요. 여기는 매 세션 반복 설명을
줄이기 위한 핵심 요약 + 세션 로그 프로세스입니다.

---

## 프로젝트 정체성 (★가장 중요)

- 목적: 40~60대 여성 대상 웰바디(다이어트/웰니스) 릴스 콘텐츠 제작 자동화
- **자동화 스코프는 0~3단계까지로 한정** (2026-08-31 확정):
  0. 트렌드/벤치마킹 수집 → 1. 대본 생성 → 2. iCloud 소스 매칭 → 3. 보이스 클로닝(TTS)
- **4~5단계(캡컷 편집·자막·BGM/SFX·검수·배포)는 전적으로 수동 진행.** 캡컷
  `draft_content.json` 자동 생성은 계획에서 제외 — 기술적 방식이 명확해지기
  전까지 재검토하지 않는다.
- 클로닝 대상 목소리: 권세미 원장. "거의 똑같게" 수준이 목표이므로 품질 기준이
  낮은 대안은 채택하지 않는다.
- 현재 단계: **Phase 1 (PoC)** — 원칙적으로 코드를 짜지 않고 사람이 품질부터
  확인하는 단계. 단, Task 1-3(트렌드 수집 스크립트)처럼 태스크 자체가 "스크립트
  v1"을 요구하는 경우는 예외적으로 코드를 작성한다.

## 절대 규칙 (예외 없음)

1. **자동 발행/자동 조립 없음** — 캡컷 조립, 자막, BGM/SFX 삽입은 사람이 직접.
   재검토 조건: CapCut 공식 자동화 경로 또는 draft_content.json 안정성 검증.
2. **비밀정보** — `config\.env`의 API 키를 응답에 노출하거나 다른 파일에
   평문으로 복사하지 않는다 (다른 프로젝트와 키를 공유 재사용하는 것 자체는
   허용 — 상세는 세션 로그/메모리 참조).
3. **병렬 진행 금지** — Phase 2부터는 한 모듈을 완료·검증한 뒤 다음 모듈로.
4. **인스타/틱톡 스크래핑 보류** — Apify 등 종량제 스크래핑은 비용/리스크
   실측 전까지 도입하지 않는다 (Phase 3 재검토 대상).

## 경로

```
C:\reels-automation\                  프로젝트 루트 (Git 저장소 아님 — 로컬 폴더)
├─ reels_automation_project_plan.md   전체 Phase/Task 계획서
├─ scripts\youtube_trends.py          Task 1-3: 유튜브 트렌드 수집 스크립트
├─ scripts\run_weekly.ps1             주간 스케줄 실행용 래퍼
├─ config\.env                        YOUTUBE_API_KEY (wellbody-blog 키 재사용, Git 제외)
├─ config\keywords.txt                벤치마킹 키워드 목록 (한 줄에 하나, # 주석)
├─ config\SESSION_LOG.md              세션 로그 — 유일한 진행상황 사본
├─ output\                            수집 결과 JSON/CSV
├─ output\briefings\                  리뷰용 마크다운 브리핑
└─ output\logs\                       스케줄 실행 로그
```

## 정기 실행 (자동, 2026-08-31 설정)

Windows 작업 스케줄러에 `ReelsAutomation_YoutubeTrends_Weekly` 작업이 등록되어
매주 월요일 오전 8:30(PC가 켜져 있을 때)에 `scripts\run_weekly.ps1`을 실행,
`config\keywords.txt` 키워드 기준으로 `youtube_trends.py`를 돌린다. 결과는
`output\`(JSON/CSV)와 `output\briefings\`(마크다운 브리핑)에 저장되고, 실행
로그는 `output\logs\`에 남는다.

이 스캔은 **트렌드 후보를 뽑아 저장하는 것까지만** 한다 — 대본 작성이나 이후
단계는 자동화 범위 밖이며, 결과 채택은 항상 사람이 한다.

**세션 시작 시**: `output\briefings\` 안에 최근 7일 이내 생성된 브리핑이 있으면
사용자가 묻기 전에 먼저 요약해서 안내한다.

## 세션 로그 프로세스 (자동)

**세션 종료 시**: 사용자가 "오늘 작업 끝", "여기까지 할게", "오늘은 여기까지",
"작업 종료", "끝낼게" 등 종료 의사를 표현하면 아래를 수행한다.

1. 이번 세션에서 완료한 작업을 항목별로 요약
2. 진행 중이거나 보류된 항목 정리
3. 다음 세션에서 시작하기 좋은 작업을 우선순위 1~3개로 추천
4. 위 내용을 `config\SESSION_LOG.md`에 **덮어쓰기**로 저장한다. 기존 내용은
   전부 삭제하고 날짜만 갱신 — 로그를 누적하지 않는다.
5. 이 프로젝트는 Git 저장소가 아니므로 커밋 절차는 없다. 저장 후 사용자에게
   저장 완료만 알린다.

**세션 시작 시**: 새 세션이 시작되어 이 CLAUDE.md를 읽는 시점에,
`config\SESSION_LOG.md` 파일이 있는지 확인한다. 있으면 사용자가 묻기 전에 먼저
아래 형식으로 안내한다.

```
지난 세션(YYYY-MM-DD) 요약:

(완료 항목)

오늘 시작하기 좋은 작업:

(추천 1)
(추천 2)
```

파일이 없으면 평소대로 진행하고 이 절차는 건너뛴다.
