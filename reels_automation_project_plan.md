# 릴스 자동화 시스템 — 실행 계획서 (Claude Code 작업용)

**목적:** 이 문서는 Claude Code 세션에서 단계별로 참조하며 작업을 진행하기 위한 브리프입니다.
Phase/Task 단위로 나눠져 있으니, 각 세션에서 해당 Task 섹션만 컨텍스트로 던져주고 진행하면 됩니다.

**핵심 원칙:** 리스크 크고 검증 안 된 모듈(캡컷 자동화, 인스타/틱톡 스크래핑)은 뒤로,
검증 쉽고 안전한 모듈(음성 품질 테스트, 유튜브 API)은 앞으로. 모듈은 병렬이 아니라 순차로 진행 —
한 모듈이 막혀도 나머지는 독립적으로 굴러가야 함.

**2026-08-31 결정사항:** 화면 편집·자막·효과음(BGM/SFX)은 기술적 방식이 더 명확해지기 전까지
**전적으로 수동 진행**으로 확정. 즉 이 시스템의 자동화 범위는 "0단계(트렌드 수집) → 1단계(대본) →
2단계(소스 매칭) → 3단계(음성 생성)"까지이며, 4단계(캡컷 조립)와 5단계(검수/배포)는 사람이
CapCut에서 직접 수행한다. 캡컷 `draft_content.json` 자동 생성은 계획에서 제외하고, 추후
기술적 방식(공식 API 등)이 명확해지면 재검토한다.

---

## 시스템 아키텍처 (최종 확정본)

```
[0. 트렌드/벤치마킹 수집]
  - 1순위: YouTube Data API (공식, 무료 쿼터, 검증됨)
  - 2순위(보류): 인스타/틱톡 — Apify 등 종량제 스크래핑, 비용/리스크 실측 후 도입
        ↓ 레퍼런스 영상 + 후킹 패턴
[1. 대본 생성 Agent]
  - 기존 대본 에이전트 재활용, 씬(Scene) 단위 JSON 스키마로 표준화
        ↓ 씬별 narration + visual_query
[2. iCloud 소스 매칭]
  - iCloud 공식 동기화 앱 → 로컬 폴더 다운로드 → CLIP/SigLIP 임베딩 → Qdrant/ChromaDB 인덱싱
  - (비공식 API 직접 접근(pyicloud) 방식은 채택하지 않음 — 2FA/ToS 리스크로 기각)
        ↓ 씬별 매칭 비디오 클립
[3. 보이스 클로닝 (TTS)]
  - ElevenLabs Creator 플랜 ($22/월, 121,000 크레딧 ≈ 121분) — 검증된 비용
        ↓ 음성 파일 + 단어 단위 타임스탬프
[4~5. 편집·자막·효과음·검수·배포] 🙋 전적으로 수동 진행 (자동화 보류)
  - 매칭된 영상 클립 + 생성된 음성을 CapCut에서 사람이 직접 조립
  - 자막, BGM, SFX 삽입, 트렌드 반영 편집 모두 수동
  - CapCut 자동화(draft_content.json)는 기술적 방식이 명확해지면 재검토
```

> 자동화 스코프는 0~3단계(트렌드 수집 → 대본 → 소스 매칭 → 음성 생성)까지로 한정한다.

---

## Phase 0 — 준비 (3~5일, 자동화 코드 없음)

- [ ] ElevenLabs Creator 플랜 가입 ($22/월)
- [ ] 와이프 목소리 샘플 녹음: 잡음 없는 환경, 5~10분 분량 WAV, 자연스러운 톤 (Professional Voice Cloning용)
- [ ] YouTube Data API 키 발급 (Google Cloud Console)
- [ ] iCloud 로컬 동기화 앱 설치 확인 — Windows PC에 실제로 사진이 로컬 폴더로 내려오는지 테스트
- [ ] Claude Code 서브에이전트 역할 초안: 기존 대본 작성 에이전트를 이 프로젝트에 재활용 가능한지 검토

**다음 단계로 넘어가는 조건:** 음성 샘플 확보 + API 키 발급 완료

---

## Phase 1 — PoC: 자동화 없이 품질부터 확인 (1~2주)

> 이 단계에서는 코드를 짜지 않고, "이 시스템이 애초에 쓸 만한가"를 사람이 직접 확인합니다.

### Task 1-1. 음성 클로닝 품질 테스트
- ElevenLabs에 샘플 업로드 → 실제 릴스 대본 3~5개로 발음/톤/숨소리 자연스러움 확인
- **판단 기준:** 실사용 가능 품질이 아니면 대안(타입캐스트 등)으로 전환 검토

### Task 1-2. 대본 생성 프롬프트 확정
씬별 JSON 스키마 표준 (아래 구조 그대로 사용 권장):
```json
{
  "total_duration_sec": 45,
  "hook_category": "문제제기형",
  "scenes": [
    {
      "scene_id": 1,
      "duration_sec": 4.0,
      "narration": "...",
      "visual_query": "검색용 키워드 설명",
      "voice_emotion": "surprised_and_friendly"
    }
  ]
}
```
- [ ] 웰바디 콘텐츠(다이어트/간헐적단식 등)로 샘플 대본 3~5개 생성해서 품질 확인
- 이 작업을 AI 교육 커리큘럼 Day 6(프롬프트 표준화)과 연결해서 진행 — 이론과 실습을 분리하지 말 것

### Task 1-3. 유튜브 트렌드 수집 스크립트 v1
- `search.list` + `order=viewCount` + `publishedAfter`(최근 N일)로 키워드별 급상승 영상 수집
- **판단 기준:** 실제로 대본 아이디어에 도움이 되는 결과가 나오는지 확인 (안 되면 소스 재검토)

### Task 1-4. 수동 엔드투엔드 테스트
- 대본 1편 + 매칭 영상(수동 선택) + 생성 음성을 사람이 직접 캡컷에 얹어 완성본 1개 제작
- 이 결과물이 실제 발행 가능한 품질인지 최종 확인 → 안 되면 Phase 2로 넘어가지 않음

---

## Phase 2 — MVP: 모듈 순차 자동화 (2주, 캡컷 자동화 제외로 기간 단축)

> 병렬 진행 금지. 한 모듈 완료·검증 후 다음 모듈로.
> 캡컷 조립은 자동화 스코프에서 제외 — Task 2-3(구 캡컷 자동 조립)은 진행하지 않는다.

### Task 2-1 (1주차). 소스 매칭 자동화
- 로컬 CLIP/SigLIP 임베딩 파이프라인 구축 (1초당 1프레임 추출 → 벡터화)
- Qdrant 또는 ChromaDB에 메타데이터(촬영일, 파일경로, 구간)와 함께 저장
- 코사인 유사도 ≥0.75 기준으로 대본 `visual_query`와 매칭
- [ ] 샘플 20개 매칭 결과로 정확도 확인

### Task 2-2 (2주차). 트렌드 수집 에이전트 정식화
- Phase 1 스크립트를 Windows 작업 스케줄러에 등록 (기존 네이버 검색광고 API 파이프라인과 동일 패턴 재사용)
- [ ] 주간 자동 수집 → 브리핑 형태로 결과 확인 가능하게 구성
- 인스타/틱톡은 이 단계에서 보류 (Phase 3로 이연)

### Task 2-3. 캡컷 조립 — 보류 (수동 진행)
- 2026-08-31 결정에 따라 이 항목은 진행하지 않음
- 2단계(소스 매칭)에서 나온 클립 목록과 3단계(음성 생성) 결과 파일을 사람이 받아
  CapCut에서 직접 조립·자막·BGM/SFX 작업
- 재검토 조건: CapCut 공식 자동화 경로가 생기거나, draft_content.json 방식의 안정성이
  별도로 검증됐을 때

**매주 체크포인트:** 그 주 작업물이 이전 모듈 없이도 독립적으로 테스트 가능한 상태인지 확인.

---

## Phase 3 — Scale-up (지속)

- [ ] 캡컷 조립 자동화 재검토 — 기술적 방식(공식 API, 더 안정적인 오픈소스 등)이 명확해지면
  Task 2-3을 다시 활성화할지 판단
- [ ] 인스타/틱톡 트렌드 자동화 도입 여부 재검토
  - Apify는 정액제 아님 — 결과 1,000건당 약 $1.50 종량제. 실제 필요 수집량으로 한 달 청구서 실측 후 결정
  - "IP 차단 위험 없음"은 벤더 마케팅 표현 — 소량 테스트로 실제 리스크 확인 후 규모 확장
- [ ] 릴스 업로드 후 조회수/저장수를 대본 프롬프트에 반영하는 피드백 루프 구축
- [ ] ElevenLabs 사용량이 Creator 플랜(121분/월) 초과 시 Pro 플랜($99) 전환 시점 계산

---

## 비용 체크리스트 (검증 상태 표시)

| 항목 | 도구 | 비용 | 검증 상태 |
|---|---|---|---|
| 대본 생성 LLM | Claude API | 사용량 기준 | 최신 모델명은 Anthropic 공식 문서에서 재확인 필요 |
| 트렌드 수집 (유튜브) | YouTube Data API | 무료 쿼터 내 | 검증됨 |
| 트렌드 수집 (인스타/틱톡) | Apify 등 | 결과 1,000건당 ~$1.50 | 정액 아님, 실사용량으로 재계산 필요 |
| 보이스 클로닝 | ElevenLabs Creator | $22/월, 121분 | 검증됨 (2026년 8월 기준) |
| 자산 검색 | 로컬 CLIP+Qdrant | $0 (로컬 컴퓨팅) | 검증됨 |
| 편집·자막·SFX | 사람이 CapCut에서 수동 진행 | $0 (본인 시간 투입) | 2026-08-31부로 자동화 보류, 확정 |

---

## 이번 주 즉시 시작 항목

1. ElevenLabs Creator 가입 + 와이프 음성 샘플 녹음
2. YouTube API 키 발급
3. 대본 JSON 스키마를 기존 대본 에이전트에 적용해 샘플 3~5개 생성
